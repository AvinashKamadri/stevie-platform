# Acronym blocker feasibility study — 2026-07-01

Auto-generated by `label.py --corpus supplement yield --archive`. Stats are
verbatim from the uniform random sample; fill in the decision below.

```
Population (acronym candidates):  1094
Random sample labeled:            80
  merge / related / distinct:     18 / 5 / 57
Estimated true-merge yield:       22.5%   95% CI [14.7%, 32.8%]  (Wilson)
Projected merges in population:   246   95% CI [161, 359]
Blocker candidate volume (emit):  1094
```

## Decision

Build the acronym blocker.

## Rationale

A random sample of 80 acronym-candidate pairs produced an estimated true-merge
yield whose 95% Wilson confidence interval projects **161–359 recoverable
organization merges** within the mined acronym-candidate population (point
estimate: 246).

The engineering decision is based on the conservative lower bound, not the point
estimate. Even under the pessimistic interpretation of the sample, the blocker is
expected to recover approximately **161 organization merges that are currently
unrecoverable**, because every confirmed acronym merge in this study is missed by
the existing blocking strategies (trigram, rare_token).

The estimated candidate cost is bounded at approximately 1,094 candidate pairs for
the mined acronym population. Based on the sampled labels, most of these candidates
are expected to be rejected by the scorer, but this review/scoring cost is small
relative to the expected increase in recoverable merges.

Additional sampling is not required for this decision. While a larger sample would
narrow the confidence interval, the current interval is already sufficiently
separated from a "do not build" outcome that further labeling is unlikely to change
the engineering decision.

## Scope

This decision applies only to the mined acronym-candidate population. It does not
estimate the prevalence of acronym merges outside the miner's detectable search
space.

## Evaluation implications

The blocker should be evaluated after implementation using the existing M4
benchmarking framework. Its contribution should transition from an *estimated*
recall gain to an *observed* recall gain measured on the versioned benchmark.

## Observed (post-implementation, 2026-07-01)

The `acronym` blocker was added to `canonical/candidates.py` and measured with
`stevie recall --corpus v2` (gold = 500 sample + 80 labeled supplement pairs):

```
Raw pairs emitted (acronym):      1,094   (matches the mined population exactly)
Net-new candidate pairs:         +1,092   (2 already covered — as mining predicted)
Marginal gold recall (acronym):     +18   (gold merges caught by NO other blocker)
Confirmed acronym merges found:   18/18   (blocking_gap on v2 fell 18 -> 0)
Blocker gold/emit efficiency:    0.0165   (best in set: trigram 0.0005, rare_token 0.0039)
```

The blocker surfaces **every** confirmed acronym merge in the benchmark, catches
them *solely* (zero overlap with the existing blockers — confirming net-new
recall, not duplication), and does so at the highest gold/emit efficiency of any
blocker. Candidate-volume cost matched the bound (+1,092).

What remains *estimated*, not observed: the absolute 161–359 population-level
recovery is still a projection — it cannot be directly observed without labeling
the full 1,094-candidate population. The benchmark confirms the blocker's
*behavior* (100% of confirmed acronym merges surfaced, best-in-set efficiency);
the population count stays a CI, and the study's lower bound (161) remains the
number the build decision rests on.
